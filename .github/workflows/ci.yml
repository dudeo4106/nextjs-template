name: ci

on:
  push:
    branches:
      - '**'

concurrency:
  group: ${{ github.ref_name }}
  cancle-in-progress: true

jobs:
  install-depencencies:
    uses: ./.github/workflow/__install-dependencies.yml@main

  lint:
    needs: install-depencencies
    uses: ./.github/workflow/__lint.yml@main

  builds:
    needs: install-depencencies
    uses: ./.github/workflow/__build.yml@main
